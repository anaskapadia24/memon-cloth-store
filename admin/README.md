# Admin panel

Vite + React + TypeScript. Calls the existing Express API.

```bash
cd admin && npm i && npm run dev   # http://localhost:5173
```

Needs a user with `role: 'admin'` in MongoDB, then sign in at `/login`.
