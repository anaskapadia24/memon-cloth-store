# Leave as /api to use the Vite proxy to the Express backend (dev).
# In production, set this to the real API origin, e.g. https://www.example.com/api
VITE_API_BASE=/api

# "View Website" link in the top bar
VITE_STORE_URL=http://localhost:3000
